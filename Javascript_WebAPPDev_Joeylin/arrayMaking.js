const colours = ['red', 'green', 'blue', 'yellow', 'purple'];

colours[0]; //"red"

colours.length;

// Important array methods:
//push(value) - adds value to the end of the array
colours.push('orange'); // adds 'orange' to the end of the array

//pop()